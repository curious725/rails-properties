version: 2.1

orbs:
  cit: chime/ci-tools@dev:99b34cd

jobs:
  build:
    docker:
      - image: cimg/ruby:2.7-node
    steps:
      - checkout
      - sonarcloud/scan



workflows:
  main:
    jobs:
      - build:
          context: SonarCloud
